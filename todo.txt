Proteomatic to do list
----------------------

Bugs

* crashes during wild file list resizing (stack overflow due to recursion)
* problems with C:\Users\Gümpel (strange paths), won't install external software
* show script description in status bar for script search results on hover
* converter script says 'no files remaining for conversion' to input file batch
* fix 'delete from here' to work with file lists and through batches & converters

Features

* minimize to system tray icon when closing
* automatically install PHP, Python, and Perl on Windows
* maybe update additional languages if new versions are available in the scripts

Script requests

* Prepend FASTA IDs with prefix
* Filter FASTA file by 'ID contains x' or ID is one of those in a text file
